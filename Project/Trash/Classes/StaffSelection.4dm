Class extends EntitySelection



Function extractOnServer( ...  : Text)->$extraction : Collection
	
	//$params:=New collection
	//For ($p; 1; Count parameters)
	//$params.push(${$p})
	//End for 
	
	//$extraction:=This.extract.apply(Null; $params)
	$extraction:=This:C1470.extract($1)