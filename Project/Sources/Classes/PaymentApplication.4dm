// Purpose: ORDA dataclass for PAY-to-INV application rows (PaymentApplication table, id 156).
// Junction table only — no SFW entry; used by SalesTransaction.applyReceivePayment.
// created by 4D/PS [2026-june-08]
Class extends DataClass
